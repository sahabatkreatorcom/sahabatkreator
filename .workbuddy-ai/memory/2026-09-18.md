# 2026-09-18 — Compose page refactoring

## AI Assistant → toggle button + popover
- `AiComposerPanel` `variant="inline"` kini render sebagai tombol pill (Sparkles icon + credit badge) yang buka popover berisi form, mirip `EmojiPicker`
- Pattern: `relative` wrapper, `absolute bottom-full` popover, `pointerdown` outside-to-close, `aria-pressed`
- Tombol AI ditempatkan next to EmojiPicker di toolbar row caption
- `VariationsAiPanel` juga disesuaikan: account selector + AI toggle inline

## Hashtag saran AI masuk dengan `#`
- Klik hashtag saran → form hashtag menerima `#tag` (dengan tanda #)
- Toggle highlight bandingkan tanpa # untuk robustness
- Diperbaiki di kedua path: inline popover + card variant

## Relokasi layout Compose
1. **Waktu Optimal**: keluar dari `ComposeTools` toolbar → panel `<details>` collapsible di sidebar kanan (di bawah Validasi)
2. **Import CSV**: keluar dari `ComposeTools` toolbar → kartu mandiri di bawah section Publikasi (editor kiri)
3. **Validasi**: dipindah ke luar scroll container preview → selalu terlihat meski preview/media panjang
4. `ComposeTools` sekarang hanya: Strategi, UTM, Sound, Produk

## Files changed
- `apps/web/src/components/compose/ai-panel.tsx`
- `apps/web/src/components/compose/variations-ai-panel.tsx`
- `apps/web/src/components/compose/compose-tools.tsx`
- `apps/web/src/pages/dashboard/compose.tsx`

## Commits
- `8733bd7` — AI assistant as toggle button + popover like emoji picker
- `e93d50e` — hashtag saran AI masuk ke form dengan tanda #
- `bf97495` — relokasi waktu optimal, import csv, dan floating validasi

## Commit
- `11f4944` — tambah link reference ke halaman Strategi di Aset Strategi

## Note
- Push gagal dari sandbox karena SSH keys di `~/.ssh/` di-block. User perlu `git push origin main` manual.

## Sound dihapus dari toolbar
- Commit `3749d2f` — SoundPicker dihapus dari ComposeTools
- Alasan: `audioTrackId` disimpan di DB tapi tidak ada publishing adapter yang membacanya (dead code)
- ComposeTools sekarang hanya: Strategi, UTM, Produk
- SoundPicker component tetap ada (bisa dipakai lagi jika adapter sudah siap)

## Generator AI upgrade (repurpose + carousel)
- Commit `11a1672`
- **Repurpose**: multi-platform (pilih beberapa target, generate paralel via Promise.all), hasil editable per platform (tab + textarea), Copy/Copy Semua/Pakai di Compose, indikator brand voice aktif, riwayat generate
- **Carousel**: export Markdown, "Pakai Caption + Outline ke Compose" (kirim caption + slide titles), indikator brand voice, riwayat generate
- **Hook baru**: `useAiHistory` — localStorage-based history (max 20 entries), restore tanpa habis credit
- Backend `/ai/repurpose` tetap single-platform; multi-platform di-handle di frontend dengan Promise.all

## AI Auto-Reply (komentar & DM) — commit f1c6e23

Rekomendasi delay-vs-implement diterapkan: DM 0.5-2 mnt, komentar 2-5 mnt,
negatif skip + human review, dry-run untuk onboarding.

Arsitektur kunci:
- `automationRule.action` jadi discriminated union: `reply` (template statis,
  kirim instan) vs `ai_reply` (tone + delayMinutes + dryRun)
- `processAutomation` (publishing/automation.ts): match → insert
  `automation_log` status `pending` + `due_at` → enqueue delayed job
- `processAutoReplyJob` (publishing/auto-reply.ts): claim atomik
  (`SET due_at=NULL WHERE status='pending'`), re-cek rule aktif &
  inbound segar (sudah-dibalas-manual → skip), sentiment guard, AI generate
  (1 kredit), kirim / simpan draft (dryRun → `engagement_item.draft_reply`)
- Queue `sk_auto_reply` (queue/auto-reply.ts): BullMQ delayed + fallback
  polling `due_at` 30s — sama pola dengan reminder
- **Cross-package constraint**: publishing TIDAK boleh import queue (cycle).
  Solusi: `queue-hook.ts` DI (`registerAutoReplyEnqueue`) — server & worker
  register saat boot. Tanpa host → fallback polling jalan otomatis
- Worker butuh AI tapi `lib/ai.ts` ada di apps/server → buat copy di
  `publishing/ai.ts` (pola yang sama dipakai `crypto.ts`: "salinan identik")

Sentiment guard: `publishing/sentiment.ts` keyword negatif ID + emosi
(tanda baca/kapital). Negatif → skip, flag `sentiment='negative'`, no credit.

Migration: `packages/db/scripts/apply-automation-ai.ts` — jalankan manual
(tanpa TTY drizzle-kit push): automation_log +engagement_item_id, +due_at,
+idx; engagement_item +draft_reply.

Frontend: automation.tsx builder mode AI; engagement.tsx banner draft
"Edit & Kirim" / "Tolak Draft" (PATCH clearDraft).
