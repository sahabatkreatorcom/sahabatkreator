# Clone fitur socaliseit ke Sahabat Kreator
Status: BERJALAN · Service: web/db/payment/queue · Diperbarui: 2026-08-19

## Sedang dikerjakan
**Fix refresh token (2026-08-19, BELUM commit/deploy)**: helper terpusat `lib/platforms/token-refresh.ts` — Meta (IG/IG_PAGE/FB/Threads) refresh pakai **access token itu sendiri** (ig_refresh_token/fb_exchange_token/th_refresh_token, `refreshAccessToken`) secara **preemptif 7 hari sebelum expiry**; platform lain refresh pakai refreshToken saat expired; update DB + `lastRefreshError`; hasil `needReconnect` saat gagal/tanpa token. `orchestrator.ts` & `inbox/sync.ts` di-refactor pakai helper. Endpoint baru `POST /api/accounts/refresh` (manual per akun, log `account.refreshed`/`account.refresh_failed`) + `GET /api/accounts/refresh` (auto semua akun workspace). UI `/connections`: auto-refresh senyap saat halaman dimuat (hanya sekali via ref, untuk akun warn/expired/lastRefreshError) + tombol **"Perbarui"** per akun (muncul saat tone warn/expired/none/lastRefreshError) + indikator progres. Typecheck web + db lolos.
**Fitur publish lengkap per API (2026-08-19, BELUM deploy)**: (1) **First comment** end-to-end untuk Instagram (`POST /{media-id}/comments` setelah publish, non-fatal), Facebook (`POST /{post-id}/comments`), YouTube (Comment Threads API). (2) **Instagram user tags + collaborators + alt text + location**: kolom baru `instagram_user_tags` & `instagram_collaborators` jsonb + `instagram_location_id` text di schema post (migrasi `0003_white_nehzno.sql` & `0004_graceful_james_howlett.sql`), payload types, posts-service insert, publish-post, publisher IG (user_tags feed image+story, collaborators feed/carousel/reel, alt_text feed image, location_id feed image+reel), UI panel Instagram di compose (lokasi terima URL `instagram.com/explore/locations/<ID>` atau ID mentah). Migrasi 0003-0004 **belum di-apply** (Neon dev + VPS). Typecheck web+db lolos. (3) Sebelumnya sesi ini: fix dialog content-tools, billing redirect `?status=` + order_id, listening items fix, editor blog rewrite, compose validation module + action publish, privacy TikTok/YouTube, Threads `crossreshare_to_ig` (migrasi 0002). **Komentar first comment, tags, collaborators, alt text, location & Threads cross-share sudah COMMIT & PUSH (ee7dae7) — belum deploy. UI/UX connections sudah COMMIT & PUSH (285703c) — belum deploy.**
**Konsolidasi kelola akun → /connections (Fix #28, PENDING deploy)**: hapus tab "Akun Terhubung" di /dashboard/settings, grup "Akun" di sidebar, dan /settings/connections; satukan jadi halaman `/dashboard/connections` (URL `/connections` via rewrite) + item nav "Koneksi akun" di grup Workspace. Hapus `account-sidebar.tsx` & `connected-accounts.tsx`. typegen + tsc lolos. **Belum deployed**.
**Sidebar grouped by function (Fix #27, PENDING deploy)**: `nav-config.tsx` grup jadi 5 fungsi (overview/content/inbox/insight/workspace) via `navGroups[]`; `sidebar.tsx` render dinamis per grup. Typecheck lolos. **Belum deployed**.
**Billing redirect (Fix #26, PENDING deploy)**: `/dashboard/billing` kini baca `?status=` (success/cancelled) → banner konfirmasi. Typecheck lolos. **Belum deployed**.
**Media delete 500 (pushed 99d17a0, PENDING deploy)**: route DELETE `/api/media` ganti `sql ANY(array)` (error Postgres → 500) → `inArray` + try/catch. Terverifikasi delete DB OK di Neon. **Belum deployed** — VPS masih kode lama (500 saat hapus media).
**Pinterest Trial access / Sandbox (Fix #24, PENDING deploy)**: env baru `PINTEREST_SANDBOX` + helper `pinterest-config.ts` (`PINTEREST_API_BASE`/`PINTEREST_TOKEN_URL`) → semua panggilan API Pinterest (OAuth token, user_account, pins, media) bisa diarahkan ke `api-sandbox.pinterest.com/v5`. Video Pin diblokir saat sandbox (batasan resmi). `.env.example` + dokumentasi. Typecheck web+env lolos. **Belum deployed** — set `PINTEREST_SANDBOX=true` saat ingin uji trial access.
**Multi-akun sosial (Fix #23, PENDING deploy)**: `/settings/connections` tak lagi membatasi 1 akun per platform — tombol selalu "Hubungkan"/"Tambah akun" + indikator jumlah akun per platform (`countByPlatform`). Backend sudah multi-akun sejak awal (`social_account` unik per org+platform+platformId; `createPosts` 1 post per akun; `/dashboard/accounts` sudah ada tombol Tambah Akun). Typecheck lolos. **Belum deployed** — perlu rebuild web; uji nyata butuh login Instagram/TikTok berbeda.
**OAuth Instagram 2 jalur (Fix #22, PENDING deploy)**: (1) Scope `INSTAGRAM` standalone diperbaiki dari scope Graph API (via FB Login, error "Invalid platform app") → scope **Instagram API with Instagram Login** (`instagram_business_*`); verified: OAuth authorize → halaman login Instagram OK. (2) Koneksi **FACEBOOK** & **INSTAGRAM_PAGE** kini 2 langkah: callback simpan token ke tabel baru `pending_oauth_session` (expires 10 menit) → redirect `?pending=<id>` → dialog pilih halaman di `/settings/connections` (via `/api/accounts/pending/[id]` GET daftar halaman+IG business, POST pilih → simpan akun dgn page access token) → hapus sesi. Migrasi `0001_hesitant_spectrum.sql` (tabel pending_oauth_session) **belum di-apply**. Typecheck web lolos. **Belum deployed** — butuh `db:migrate` di Neon+VPS + rebuild web.
**Admin polish (Fix #21, PENDING deploy)**: 7 item admin beres — (1) `/api/admin/billing/stats` kini query DB nyata (totalRevenue, monthlyRevenue, revenueLastMonth, activeSubscriptions, totalCustomers, churnRate dari `payment` + `subscription`; sebelumnya mock hardcoded). (2) Sidebar admin tambah item `Blog` → `/admin/blog` (editor sudah ada, hanya tanpa nav). (3) `/admin/platforms` API + UI kini tampilkan `callbackUrl` (`/api/accounts/callback/<platform>`) & `webhookUrls` per platform. (4) Dropdown aksi `/admin/users` tak lagi terpotong (hapus `overflow-hidden` wrapper tabel). (5) Nav "Overview" hanya aktif saat persis `/admin` (`isActive` exact-match). (6) **SumoPod Pay punya UI admin**: route baru `/api/admin/sumo-pod` (GET masked + PUT upsert `globalIntegrationSettings`, secret di-`encryptToken`) + kartu `SumoPodPaySettingsCard` di `/admin/billing` (API key/secret, webhook secret/token, base URL, trial days, toggle); `packages/payment/src/encryption.ts` baru + `sumopod.ts` kini `decryptToken` saat baca config; payment +dep `@sahabat-kreator/env`. `pnpm install` + `pnpm --filter web check-types` lolos. **Belum deployed**.
**UI/UX dashboard polish (2026-08-19)**: (1) `nav-config.tsx` "Ringkasan" href `/` → `/dashboard` (tetap satu-satunya yang panjang; sisanya URL pendek). (2) `next.config.ts` tambah 4 rewrite hilang: `/listening`, `/status`, `/pillars`, `/billing` → `/dashboard/*` (sebelumnya 404). (3) Komponen baru `ui/password-input.tsx` (tombol lihat/sembunyikan, `Eye`/`EyeOff`), dipakai di login & register. (4) Org switcher dipindah dari atas aside ke **bawah logo platform** di `sidebar.tsx` (props `organizations`+`activeOrganizationId` diteruskan dari layout; import `OrgSwitcherProps` diekspor dari org-switcher). Typecheck web lolos. **Belum deployed**.
**Redirect post-login (2026-08-19)**: 3 tempat `router.push("/")` → `/dashboard` (new-workspace-form, accept-invitation-actions, verify-2fa-form fallback). Typecheck lolos. **Belum deployed**.
**Email template profesional (2026-08-19)**: `packages/email/src/templates.ts` baru — layout branded (header gelap + aksen #D4A574, kartu, footer, copyright), CTA button + fallback link, escapeHtml, opsi OTP box. 4 call-site di `auth.ts` (verifikasi email, reset password, undangan org, OTP 2FA) kini pakai `emailTemplates`. Typecheck lolos. **Belum deployed**.
**better-auth downgrade (Fix #19, PENDING deploy)**: 1.7.0 (beta, wajib `account.issuer`) → 1.6.30 stable via catalog `~1.6.29`; API 2FA `enable` disesuaikan. Typecheck lolos.

## Status terakhir
- **✅ DEPLOY LIVE (Fix #10–#17, 2026-08-19)**: semua entri CHANGELOG ditandai LIVE. Rangkaian error Docker berurutan yang diselesaikan: (a) Fix #13 build args (ENCRYPTION_KEY + R2_* + args migrate), (b) Fix #14 Resend lazy-init, (c) Fix #15 prerender blog/sitemap force-dynamic, (d) Fix #16 migrate stage dari deps (EACCES corepack), (e) Fix #17 @swc/helpers copy utuh (web crash). DNS Cloudflare: **DNS only (grey cloud)** — Caddy serve SSL langsung. SSL Cloudflare mode Full. Catatan DNS: HTTP-01 challenge Let's Encrypt gagal saat Proxied; grey cloud diperlukan. Caddy certificate obtained via acme HTTP-01.
- **Worker hemat RAM + fallback poster-only (baru, Fix #11)**: video > `WORKER_MAX_VIDEO_BYTES` kini tetap diproses → poster saja (ffmpeg 1 frame, tanpa transcode) status `LIMITED`; yang ≤ batas tetap `DONE` penuh (frames + transcode). Query `findPendingVideos` tidak lagi memfilter `size <=` (sebelumnya video besar tak pernah diambil). Default baru untuk VPS kecil: `WORKER_BATCH_SIZE=1`, `WORKER_POLL_INTERVAL_MS=15000` (compose & .env.example); concurrency BullMQ `publishWorker` 3→1, `syncWorker` 2→1 di `apps/web/src/lib/queue-worker.ts`. DEPLOYMENT.md: bagian "Konfigurasi hemat RAM" + langkah swap 2 GB. `pnpm -r check-types` lolos.
- **Fix prerender blog/sitemap (baru, Fix #15)**: `next build` gagal di `/blog` — `ENOTFOUND postgres` karena halaman blog & sitemap di-prerender saat build padahal baca DB (host postgres belum ada saat build). Fix: `export const dynamic = "force-dynamic"` di `(marketing)/blog/page.tsx`, `(marketing)/blog/[slug]/page.tsx`, `sitemap.xml/route.ts`. `pnpm -r check-types` lolos. **Belum diverifikasi** — rebuild di VPS.
- **Fix Resend lazy-init (baru, Fix #14)**: `next build` gagal di `/accept-invitation/[id]` — `new Resend(process.env.RESEND_API_KEY)` di module scope `packages/email/src/index.ts` throw saat key kosong. Fix: lazy `getResend()` (instance dibuat saat `sendEmail` dipanggil), ekspor `resend` langsung dihapus. `pnpm -r check-types` lolos. **Belum diverifikasi** — rebuild di VPS.
- **Fix Docker build args (baru, Fix #13)**: `docker compose up -d --build` gagal — `next build` menolak env kosong (`ENCRYPTION_KEY`, `R2_*`, dan semua ARG saat service `migrate`). Fix: `apps/web/Dockerfile` tambah ARG+ENV `ENCRYPTION_KEY`+`R2_*` (5 var), `docker-compose.yml` tambah blok `build.args` lengkap ke `migrate` (sebelumnya tanpa args) & `web`. **Belum diverifikasi** — user harus rebuild di VPS; pastikan `.env` di VPS punya `ENCRYPTION_KEY` (base64 32B) + `R2_*`.
- **Kontrak Transcoder (baru, Fix #12)**: interface `Transcoder` di `apps/worker/src/transcoder/` (`types.ts` + `local.ts` ffmpeg + `index.ts` resolver env `TRANSCODER` default "local", fail-fast bila "modal" belum ada). Worker loop memakai `transcoder.transcode()` — worker loop tak berubah kalau nanti Modal ditambahkan (tinggal `transcoder/modal.ts` + daftarkan di resolver). Env `TRANSCODER=local` di `.env.example` + compose. `pnpm -r check-types` lolos.
- **Fresh VPS guide (baru)**: `docs/DEPLOYMENT.md` bagian `## 0. Fresh VPS — Langkah Demi Langkah` — 7 langkah (update+Docker, swap 2GB, ufw 22/80/443, DNS A record, clone+.env, `docker compose up -d --build`, update) + perintah verifikasi tiap langkah.
- **Worker hemat RAM + fallback poster-only (baru, Fix #11)**: video > `WORKER_MAX_VIDEO_BYTES` kini tetap diproses → poster saja (ffmpeg 1 frame, tanpa transcode) status `LIMITED`; yang ≤ batas tetap `DONE` penuh (frames + transcode). Query `findPendingVideos` tidak lagi memfilter `size <=` (sebelumnya video besar tak pernah diambil). Default baru untuk VPS kecil: `WORKER_BATCH_SIZE=1`, `WORKER_POLL_INTERVAL_MS=15000` (compose & .env.example); concurrency BullMQ `publishWorker` 3→1, `syncWorker` 2→1 di `apps/web/src/lib/queue-worker.ts`. DEPLOYMENT.md: bagian "Konfigurasi hemat RAM" + langkah swap 2 GB. `pnpm -r check-types` lolos.
- **SSL/HTTPS produksi (baru, Fix #10)**: `Caddyfile` baru (auto TLS Let's Encrypt via `{$DOMAIN}`, redirect 80→443, `reverse_proxy web:3000`, HSTS). `docker-compose.yml`: service `caddy` + volume `caddy-data`/`caddy-config`; hapus `ports` publik postgres & web (kini hanya di network compose); `depends_on web: service_healthy`. `.env.example`: tambah `DOMAIN`, hapus `WEB_PORT`/`POSTGRES_PORT`. DEPLOYMENT.md: bagian "DNS & SSL" (A record → IP VPS, buka 80/443), arsitektur + renumber 2–8. Verifikasi YAML struktural OK; `docker compose` belum diuji (docker tidak tersedia di mesin dev).
- **Next standalone (baru, Fix #9)**: `output: "standalone"` + `outputFileTracingRoot` (root workspace) di next.config.ts; `apps/web/Dockerfile` runner kini salin `.next/standalone` (struktur berprefix `apps/web/`), `CMD node server.js`, cwd `/app/apps/web`, aset `.next/static`+`public` di-copy. Docker build belum diuji (docker tak ada di mesin dev).
- **Kredensial platform (baru, Fix #8)**: `getCredentialsForPlatform` kini env→DB hybrid (env `{KEY}_CLIENT_ID/SECRET`, FACEBOOK/INSTAGRAM_PAGE→META). Baru `lib/webhooks/secrets.ts` — `getWebhookSecretConfig(platform)` baca DB `global_platform_credential` dulu (clientSecret decrypt + webhookVerifyToken), fallback env (`META_APP_SECRET`, `*_CLIENT_SECRET`, `WEBHOOK_VERIFY_TOKEN`). Semua verify webhook (meta/tiktok/youtube + 4 route) jadi async & pakai config terpadu. Admin baru: `/api/admin/platform-credentials` (GET masked + PUT upsert, secret `encryptToken`, kosong=pertahankan) + halaman `/admin/platforms` + link sidebar. Kredensial platform kini bisa diisi → siap pengajuan akses API (Meta/TikTok/Google). Privacy/Terms/Data Deletion ada di `(marketing)/kebijakan-privasi|syarat-ketentuan|penghapusan-data`.
- **Single env source of truth (baru, Fix #7)**: satu `.env` + satu `.env.example` di **root**. `packages/env/src/load.ts` (baru) — `loadRootEnv()` naik dari CWD ke root (`pnpm-workspace.yaml`) lalu memuat `.env`; dipakai `server.ts`, `web.ts`, dan `drizzle.config.ts`. File `.env` dev dipindah apps/web → root; `.env.example` per-app dihapus (apps/web & apps/worker); `.env.e2e` tetap di apps/web (khusus Playwright, gitignored). `next.config.ts` meng-import `@sahabat-kreator/env/web` → root env ter-load otomatis saat dev/build.
- **Bersih-bersih menyeluruh (baru, Fix #6)**: hapus dead code `lib/logger.ts`(+evlog), `src/emails/`(+react-email), `lib/email.ts`(+@sahabat-kreator/email di web), `lib/seo-helper.ts`, `hooks/use-push-notifications.ts`, `packages/env/src/native.ts`; route API mati `/api/blog/*` + `/api/admin/billing/plans`; dep `playwright`(web), `@better-auth/drizzle-adapter`(auth), root `@sahabat-kreator/env`/`dotenv`/`zod`. Env `SUMOPOD_*` dihapus dari schema+example (konfigurasi dari DB `global_integration_settings`). **Fix bug** `admin/blog/new/page.tsx` self-import (kini render `../[id]/page`); `admin/stats` kini hitung DB nyata (sebelumnya angka palsu). **Maintainability**: wrapper `withAdmin`/`withOrgOwnerAdmin` di lib/api.ts + migrasi 12 route admin/blog/billing; `biome.json` dasar + `pnpm check` (biome); `check-types` packages/env; standardisasi `json()` helper. **Postgres Docker produksi**: `docker-compose.yml` + service `postgres`(16-alpine, volume, healthcheck) + `migrate` (sekali-jalan, web/worker `depends_on service_completed_successfully`); `.env.example` root baru (POSTGRES_* + DATABASE_URL); dev tetap Neon (`?sslmode=require`). **Catatan**: package.json sempat rusak BOM/format oleh ConvertTo-Json — sudah ditulis ulang rapi & dihapus BOM; `lint` script dihapus (biome recommended → 48k format error; pakai `pnpm check` saja).
- **Audit ke-3 hardening (baru, Fix #5)**: TikTok webhook diperbaiki ke format resmi (`t=,s=`, HMAC `"${ts}.${body}"`, freshness 5m, envelope `content`/`user_openid`); idempotency webhook jadi atomik (INSERT ON CONFLICT); body limit 1MB; Meta eventId org-scoped + hapus `message_reactions`; SumoPod webhook: Svix freshness + timing-safe token + transaksi atomik + idempotency + cek amount + anti-downgrade + perpanjangan currentPeriodEnd; billing hanya owner/admin (`requireOrgOwnerAdmin`) + anti-downgrade; **cron baru `/api/cron/billing`** (downgrade tier expired → FREE); harga BUSINESS 249k (satu sumber: PLAN_PRICES); CSP media-src + fonts; `<video>` src fix; root `.dockerignore`; env schema +RESEND/GITHUB/LOG_LEVEL; bersihkan dep tak terpakai (dotenv→devDeps, config web, env payment).
- **Webhook platform (baru, Fix #4)**: `lib/webhooks/` (index.ts = verifikasi HMAC + idempotency atomik via `processed_webhook_event`; meta.ts = handler IG/FB/Threads; tiktok.ts; youtube.ts). Route terpisah per platform (Meta: `object:instagram` IG, `object:page` FB, `object:threads` Threads — callback URL masing-masing): `api/webhooks/{instagram,facebook,threads,tiktok,youtube}`. Wire event: komentar IG/FB/Threads → upsert inbox (`upsertIncomingComment` di export dari `lib/inbox/sync.ts`) + auto-reply; TikTok `post.publish.complete/fail` → update post status/publish_error; YouTube PubSubHubbub feed → aktivitas org. Env baru `META_APP_SECRET` + `WEBHOOK_VERIFY_TOKEN`. **Penting**: Pinterest/LinkedIn/Google Business tidak punya webhook publik → tetap polling.
- **Icon platform (baru)**: `components/ui/platform-icon.tsx` dari `simple-icons` (path SVG brand, tree-shaken). Dipakai di footer + 8 halaman dashboard + connections (ganti inisial/lingkaran teks). LinkedIn & MANUAL tidak ada di simple-icons → fallback inisial.
- **SumoPod Pay DIWIRE (baru)**: `@sahabat-kreator/payment` kini dipakai — `api/billing` GET (plan limits nyata via `getPlanLimits` + `PLAN_PRICES` baru di billing.ts) + POST checkout (buat payment SumoPod → return `checkoutUrl`), dashboard billing redirect ke payment link (bukan alert demo). Webhook route baru `api/billing/webhook/route.ts` (verifikasi Svix/X-Webhook-Token via raw body → `handleWebhook`). `handleWebhook` kini mengaktifkan tier org (PRO/BUSINESS/ENTERPRISE + subscriptionStatus + currentPeriodEnd +1 bulan) saat `payment.completed`. **Kolom Stripe DIHAPUS**: `organization.stripeCustomerId/SubscriptionId/PriceId` → `sumopodCustomerId`; `globalIntegrationSettings.stripe*` dibuang + `stripeTrialDays` → `sumopodTrialDays`. Migrasi diregenerate (`0000_adorable_madame_web.sql`, 82 tabel, 0 stripe). **Konfigurasi SumoPod di DB** (`global_integration_settings`) — env SUMOPOD_* dihapus (Fix #6).
- **Audit ke-2 hardening (baru)**: (1) `.gitignore` root kini `.env*` + `!.env.example` (sebelumnya `.env.prod`/`.env.e2e` di root & apps/worker TIDAK ter-ignore — risiko secret ter-commit). (2) `api/status` bocor `publishError` lintas-org DIPERBAIKI (join ke post + filter organizationId). (3) `blog_post_tag` kini composite PK `(post_id, tag_id)` (sintaks lama `[{pk, column}]` invalid → tabel tanpa PK) + index tag. (4) Index baru: `media (transcode_status, created_at)` untuk worker polling, `post (status, scheduled_at)` untuk cron global, `invitation_inviter_id`, `team_organization_id`, unique `member(org,user)`. (5) **timestamptz** untuk kolom yang dibandingkan di JS: `post.scheduled_at`, `post.published_at`, `social_account.token_expiry`, `blog_post.published_at` (sebelumnya timestamp naive → pergeseran zona bisa bikin token dianggap expired / jadwal meleset). (6) `error.tsx` + `not-found.tsx` untuk route group `(marketing)`. (7) CSP + HSTS + frame-ancestors di next.config; hapus rewrite `/` yang dead. (8) `sharp` pindah devDeps → **dependencies** (dipakai runtime next/image). (9) `typescript` web disamakan `^6.0.3`. (10) `R2_*` kini WAJIB di env schema + `lib/storage.ts` pakai `@sahabat-kreator/env` (bukan `process.env!`) + `getPublicUrl` otomatis pakai `R2_CUSTOM_DOMAIN`. (11) `drizzle.config.ts` fail-fast tanpa DATABASE_URL + opsi `DATABASE_SSL`. (12) `packages/email` tambah `@types/react`. (13) `robots.ts` pakai `NEXT_PUBLIC_APP_URL` (bukan NEXT_PUBLIC_BASE_URL). (14) `instrumentation.ts` tidak lagi `process.exit` saat shutdown. (15) blog publik GET by slug & by tag kini filter status PUBLISHED. (16) `authorId` admin blog = `session.user.id` (sebelumnya salah simpan orgId). (17) rewrite `/` dihapus (landing filesystem menang).
- **Job queue BullMQ (baru)**: paket `packages/queue` — queue `publish-post` & `sync` (analytics/inbox), helper `enqueuePublishPost` (delay-based, jobId deterministik `post:<id>`), `removePublishJob`, `enqueueSync`. Worker BullMQ berjalan **di dalam proses web** via `apps/web/src/instrumentation.ts` → `lib/queue-worker.ts` (guard: runtime node + REDIS_URL terisi + QUEUE_WORKER_ENABLED != false). Enqueue otomatis di `posts-service.ts` (createPosts/updatePost/deletePost). `REDIS_URL` ditambahkan ke env schema + `.env.example` + `docker-compose.yml` (service `redis:7-alpine` AOF). Fallback tetap: cron DB polling `/api/cron/publish`. **Menambah platform baru = cukup tambah publisher di `lib/publishing/`, infra queue tak berubah.** `ioredis` direct dep di queue dihapus (BullMQ bawa sendiri).
- **Production hardening (baru)**: (1) POST publik di `api/blog/posts/[slug]` DIHAPUS (hanya GET publik; sebelumnya siapa pun bisa insert blog post). (2) `/api/cron/publish`, `/api/analytics/sync`, `/api/inbox/sync` kini **fail-closed** via `verifyCronSecret` di `lib/api.ts` (timingSafeEqual; tolak bila CRON_SECRET kosong — sebelumnya fail-open bila env kosong). (3) `admin/blog/posts` + `[id]` kini cek role admin (`requireAdmin` baru di lib/api.ts). (4) Anti-SSRF diperkuat: `stock-media/import` allowlist domain (pixabay/pexels/unsplash) + `seb-advisor` resolve DNS & blokir IP privat/IPv6-mapped/metadata (`assertPublicResolvedIp`). (5) `/api/health` kini cek DB (`select 1`, 503 bila gagal); `/api/admin/health` pakai metrik nyata (process.memoryUsage/uptime) bukan hardcode. (6) `NEXT_PUBLIC_APP_URL` divalidasi di `packages/env/src/web.ts` + di-ARG build Dockerfile + dipakai sitemap/robots (bukan NEXT_PUBLIC_BASE_URL lagi). (7) `ENCRYPTION_KEY` WAJIB di production (superRefine). (8) Security headers + poweredByHeader:false + cache immutable untuk `/_next/static` di next.config.ts. (9) `organization.created_at` kini `defaultNow()`. (10) warning peer better-auth hilang (tambah `@better-auth/utils@0.4.2` di packages/auth).
- **Migrasi DB diregenerasi**: `0000_low_james_howlett.sql` (82 tabel) — menggantikan `0000_cloudy_rachel_grey.sql`. Perbaiki duplikasi unique `blog_post_slug_unique` (constraint + index nama sama → migrasi gagal di DB baru; kini hanya uniqueIndex). **Belum pernah di-apply ke DB.** Bila DB lama sudah terlanjur migrate dengan file lama, jangan squash — buat migrasi delta.
- **Dependensi dibersihkan**: hapus `@base-ui/react`, `@radix-ui/react-label`, `@tanstack/react-form/query/table/virtual`, `@uppy/*`, `class-variance-authority`, `ioredis`, `motion`, `next-themes`, `sonner`, `zustand`, `tw-animate-css`, `resend`, `zod` (web); `@vitest/coverage-v8/ui`, `@testing-library/*`, `jsdom`, `sharp` (devDeps); `zod`/`dotenv` worker; `zod` db/payment; `zod`/`dotenv` auth. **`sharp` dihapus** — verifikasi image optimization Next tetap jalan di prod; kalau error, kembalikan.
- **Halaman public & marketing (baru)**: landing page, /fitur, /harga (pakai plan limit nyata Free/Pro/Business/Enterprise), /tentang, /faq, /karir, /kontak, /kebijakan-privasi, /syarat-ketentuan, /penghapusan-data (dipindah ke route group `(marketing)`) — semua pakai Header/Footer dari `(marketing)/layout.tsx`. **Header kini sticky** (backdrop-blur, highlight nav aktif). Blog pages tanpa header/footer inline (duplikat dihapus).
- **typedRoutes DIHIDUPKAN kembali** (`next.config.ts`) + script `check-types` web kini `next typegen && tsc --noEmit` (agar fresh clone tak menipu). Halaman baru dibuat agar tidak ada link mati. **PENTING**: CI/fresh clone harus punya env (typegen load next.config) — Docker build sudah melempar DATABASE_URL/BETTER_AUTH_*/CORS_ORIGIN/NEXT_PUBLIC_APP_URL.
- **Sitemap**: route dipindah ke `app/sitemap.xml/route.ts` (URL `/sitemap.xml` konsisten dengan robots.ts).
- **Docker deployment baru (fix)**: `apps/web/Dockerfile`, `apps/worker/Dockerfile`, `.dockerignore`, `docker-compose.yml` ditulis ulang — pnpm 10 full workspace install (bukan `--filter` saat install), ffmpeg di worker image, redis service, healthcheck cek DB. Lihat `docs/DEPLOYMENT.md`.
- **Dokumentasi (baru)**: `README.md` (arsitektur, setup, fitur) + `docs/DEPLOYMENT.md` (deploy VPS, migrasi, cron, queue).

## Keputusan penting
- (tetap) Semua poin keputusan lama berlaku.
- **Scheduling memakai BullMQ** (delay-based, in-process worker via instrumentation) sebagai jalur utama; `/api/cron/publish` jadi fallback/safety-net. Tidak pakai ioredis langsung — Redis hanya via BullMQ.
- **Cron fail-closed**: semua endpoint cron tolak bila `CRON_SECRET` kosong. `CRON_SECRET` wajib di production.
- `ENCRYPTION_KEY` wajib di production (jangan bergantung fallback derive BETTER_AUTH_SECRET).
- Route tulis admin wajib `requireAdmin`; route tulis user wajib `withAuth`/`requireAuth`.

## Langkah berikutnya
1. **Fix refresh token (baru)**: setelah puas → `pnpm --filter web exec tsc --noEmit` → commit + push → deploy VPS. Verifikasi live: akun Meta yang `lastRefreshError` / token hampir habis ter-refresh otomatis saat `/connections` dibuka (tanpa klik apapun), tombol "Perbarui" bekerja, dan publish tetap jalan tanpa error TOKEN_EXPIRED.
2. **Tuntaskan fitur publish per API**: musik TikTok TIDAK didukung API resmi — SKIP (tidak ada param di `post_info`; hanya pihak ketiga berbayar). Fitur lain (mention via caption native, location) sudah selesai. Migrasi 0001/0002/0003/0004 belum di-apply — `db:migrate` di Neon dev & VPS.
3. **Uji upload video di prod** (`/dashboard/media`): verifikasi worker ffmpeg di VPS → thumbnail + transcodedUrl terisi (log `[worker] done`). **Ini belum diuji live** — Fix #11 (poster-only & batch=1) belum terverifikasi runtime.
3. Uji live E2E SEB: isi OPENROUTER_API_KEY → generate laporan/chat/scan/analisis media di `/dashboard/seb`.
3. Uji live publish: hubungkan akun, jadwalkan post, pastikan job queue `publish-post` terproses (log `[queue-worker] publish done`).
4. Uji live pembayaran SumoPod: isi config di global integration settings (admin) → klik Upgrade di `/dashboard/billing` → bayar QRIS → pastikan webhook `/api/billing/webhook` menerima `payment.completed` → tier org naik.
5. **Uji live webhook platform**: set `META_APP_SECRET` + `WEBHOOK_VERIFY_TOKEN` (atau isi Platform Credentials di `/admin/platforms`), daftarkan callback di App Dashboard Meta (IG: `/api/webhooks/instagram`, FB: `/api/webhooks/facebook` — ditampilkan di kartu Meta; Threads: `/api/webhooks/threads` di kartu Threads) + TikTok portal + YouTube hub → komentar real-time masuk inbox, publish TikTok update status.
6. Uji live competitors (IG Business), inbox sync, auto-reply.
7. Isi kredensial OAuth global via admin/DB supaya tombol Hubungkan berfungsi.
8. Konfigurasi cron safety-net: `/api/cron/publish` (tiap menit) + `/api/cron/billing` (tiap jam) + `/api/analytics/sync` + `/api/inbox/sync` (tiap jam, + `x-organization-id`).
9. **R2_CUSTOM_DOMAIN** opsional (bagian "8. Skala") — set kalau mau URL publik media pakai domain sendiri.
10. Verifikasi image optimization Next di prod (sharp sekarang di dependencies — aman).

## Jangan lakukan
- (tetap) Semua larangan lama berlaku.
- Jangan aktifkan **"Always Use HTTPS"** di Cloudflare Edge Certificates — bisa menggagalkan HTTP-01 renew Let's Encrypt lewat proxied.
- Jangan set SSL Cloudflare ke Flexible (origin harus selalu HTTPS; Caddy hanya serve 443).
- Kalau renew acme gagal suatu saat (`docker compose logs caddy`), jangan langsung ubah Caddyfile — coba grey-cloud sementara dulu (5 menit) untuk renew, baru proxied lagi. Opsi permanen: Cloudflare Origin CA cert.
- Jangan ubah file CHANGELOG_FIXES.md pakai PowerShell `Set-Content`/`ConvertTo-Json` (BOM/encoding mangle) — pakai tool edit/Write.
- Jangan enqueue job queue dari route/serverless lain — hanya via `posts-service.ts` (create/update/delete).
- Jangan mengubah jobId `post:<id>` — dipakai untuk mencegah job ganda.
- Jangan jalankan worker ffmpeg di proses web (serverless/Next) — tetap di `apps/worker`.
- Jangan memakai `REDIS_URL` langsung dengan ioredis di kode app — selalu lewat `packages/queue`.
- Jangan apply migrasi squash `0000_adorable_madame_web.sql` ke DB yang sudah migrate dengan file lama (konflik CREATE TABLE).
- **Jangan hidupkan kembali Stripe** — pembayaran WAJIB SumoPod (`@sahabat-kreator/payment`). Semua kolom/settings stripe sudah dihapus dari schema.
- Jangan panggil webhook SumoPod dengan body ter-parse (JSON.parse ulang) — `verifyWebhook` butuh raw body; satu karakter whitespace berubah = signature gagal.
- **Jangan gabungkan webhook Meta** — Instagram/Facebook/Threads punya callback URL sendiri (`object` berbeda). Jangan parse ulang body sebelum verifikasi signature.
- Jangan pakai lucide-react untuk icon brand sosial (sudah dihapus dari library) — wajib `PlatformIcon` (simple-icons).
